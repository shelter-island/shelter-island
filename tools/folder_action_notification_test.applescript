on adding folder items to this_folder after receiving added_items
	set itemCount to count of added_items
	display notification ((itemCount as text) & " file(s) added to PNG IN") with title "Folder Action Test"
end adding folder items to
