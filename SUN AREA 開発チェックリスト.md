# SUN AREA 開発チェックリスト

SUN AREAの完成状態を確認するための簡易チェックリスト。

## 現在の実装状況

- [x] 住人表示
- [x] 自律移動と手動Move
- [x] 性格による行動補正
- [x] お気に入りスポット
- [x] 行動履歴（住人ごとに最大10件）
- [x] `areaMood`
- [x] mood履歴（最大10件）
- [x] 住人の痕跡（最大4件）
- [x] 環境イベントと内部履歴（最大5件）
- [x] 開発用「今日のSUN AREAカード」
- [x] 開発用デバッグ表示

## 画面確認

開発サーバーを起動する。

```bash
npm run dev
```

### 住人

- [ ] SUN住人の画像が島の上に表示される
- [ ] 住人が島の中を自動で移動する
- [ ] Move選択後に島をタップすると住人が移動する
- [ ] 住人が島の外へはみ出さない
- [ ] 木、dock、ベンチ、ライトなどへ移動する
- [ ] お気に入りスポットで少し長く過ごす
- [ ] 性格による移動先や滞在時間の違いが動作している

### 履歴とmood

確認中だけ`src/js/residentConfig.js`の`SHOW_RESIDENT_DEBUG`を`true`にする。

- [ ] resident IDと最近の行動履歴が表示される
- [ ] memory summaryが表示される
- [ ] 現在のmoodとmood scoreが表示される
- [ ] moodが変わった時だけmood履歴が増える
- [ ] area memory summaryが表示される

確認後は必ず戻す。

```jsx
export const SHOW_RESIDENT_DEBUG = false;
```

### 痕跡と環境イベント

- [ ] 住人の滞在後、低頻度で小さな痕跡が現れる
- [ ] 痕跡が数秒後に自然に消える
- [ ] 海風、木揺れ、夜の静けさ、光の反射、流れ星が低頻度で発生する
- [ ] 環境イベントが数秒後に自然に消える
- [ ] デバッグ表示で環境イベント履歴を確認できる
- [ ] 痕跡や環境イベントがボタン操作を邪魔しない

### 今日のSUN AREAカード

確認中だけ`SHOW_AREA_MEMORY_CARD`を`true`にする。

- [ ] 画面下部に「今日のSUN AREA」が表示される
- [ ] まとめ候補が1〜3行で表示される
- [ ] ボタンや島の操作を邪魔しない

確認後は必ず戻す。

```jsx
export const SHOW_AREA_MEMORY_CARD = false;
```

## iPhone確認

ブラウザ表示を390×844にする。

- [ ] 文字やボタンが画面外へはみ出さない
- [ ] 住人、痕跡、環境イベントが見える
- [ ] デバッグ表示が操作を邪魔しない
- [ ] 今日のSUN AREAカードが他のUIと重ならない
- [ ] 移動やアニメーションが重くない

## 最終確認

```bash
npm run build
git diff --check
```

- [ ] `npm run build`がエラーなく完了する
- [ ] `git diff --check`が何も表示せず完了する
- [ ] 開発用フラグが両方`false`になっている

## 関連リンク

- [[SUN住人追加マニュアル]]
- [[SUN AREA v0.1 記録]]
- [[THE SUN AREA]]
